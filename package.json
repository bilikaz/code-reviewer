{
  "name": "@bilikaz/code-reviewer",
  "version": "0.2.0",
  "description": "LLM-driven PR reviewer. Pluggable VCS destinations. OpenAI-compatible LLM API.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "bin": {
    "reviewer": "dist/cli.js"
  },
  "files": ["dist"],
  "publishConfig": { "access": "public" },
  "scripts": {
    "start": "node --experimental-transform-types --disable-warning=ExperimentalWarning src/cli.ts",
    "build": "tsc -p tsconfig.build.json && node scripts/copy-assets.mjs",
    "typecheck": "tsc --noEmit",
    "test:inner": "vitest run",
    "test": "docker build --build-arg BUILD_TARGET=test -t reviewer-test . && docker run --rm --entrypoint pnpm --env-file tests/.env -v \"$PWD/node_modules:/reviewer/node_modules\" -w /reviewer reviewer-test run test:inner"
  },
  "dependencies": {
    "@octokit/rest": "^21.0.2",
    "ajv": "^8.17.1",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3",
    "vitest": "~4.1.4"
  },
  "engines": {
    "node": ">=24"
  }
}
