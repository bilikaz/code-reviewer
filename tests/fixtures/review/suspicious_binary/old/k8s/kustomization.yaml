apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

namePrefix: prod-

commonLabels:
  app: api
  env: production

images:
  - name: api
    newTag: v1.4.2
