node_modules/
.env
.env.local
*.log
*.sh

# Agent harness — developed here for context continuity, will be moved to its
# own repo later. Has its own git; kept out of the reviewer repo.
harness/

# Agent/editor local config (permission grants etc.) — never committed
.claude/
.qwen/
CLAUDE.local.md

# Session alignment doc — scratchpad, never committed (see CLAUDE.md)
implementation.md
